# فروشگاه کمبو 🛒

یک فروشگاه آنلاین استاتیک برای فروش کمبوهای خوشمزه که با HTML، CSS و JavaScript خالص ساخته شده است. این پروژه کاملاً استاتیک است و می‌تواند بدون نیاز به سرور یا بک‌اند روی GitHub Pages اجرا شود.

## 📁 ساختار پروژه

```
AbolStoRe2/
│
├── frontend/           # فایل‌های فرانت‌اند
│   ├── index.html      # صفحه اصلی سایت
│   ├── styles.css      # فایل استایل‌های CSS
│   ├── app.js          # فایل JavaScript اصلی
│   ├── products.json   # فایل داده محصولات
│   ├── README.md       # راهنمای فرانت‌اند
│   └── assets/        # پوشه تصاویر
│
└── backend/           # پوشه بک‌اند (فعلاً خالی)
    └── README.md      # توضیحات بک‌اند
```

## ✨ ویژگی‌ها

- ✅ نمایش محصولات از فایل JSON
- ✅ سبد خرید با localStorage
- ✅ Modal نمایش جزئیات محصول
- ✅ فرم ثبت سفارش کامل
- ✅ اتصال به Formspree برای دریافت سفارشات
- ✅ طراحی کاملاً ریسپانسیو
- ✅ سازگار با موبایل
- ✅ بدون نیاز به سرور یا بک‌اند

## 🚀 نحوه اجرای پروژه

### روش 1: اجرای محلی

1. به پوشه `frontend` بروید:
   ```bash
   cd frontend
   ```

2. فایل `index.html` را با دابل کلیک باز کنید

3. یا از یک سرور محلی استفاده کنید:
   ```bash
   # با Python
   python -m http.server 8000
   
   # با Node.js (http-server)
   npx http-server
   ```

4. مرورگر را باز کنید و به آدرس `http://localhost:8000` بروید

### روش 2: اجرا روی GitHub Pages

1. پروژه را در یک repository روی GitHub آپلود کنید
2. به Settings → Pages بروید
3. Source را روی `main` (یا `master`) و `/frontend` تنظیم کنید
4. Save کنید
5. بعد از چند دقیقه، سایت شما در آدرس زیر در دسترس خواهد بود:
   ```
   https://YOUR_USERNAME.github.io/REPOSITORY_NAME
   ```

## 📝 نحوه افزودن یا تغییر محصولات

محصولات در فایل `frontend/products.json` ذخیره می‌شوند. برای افزودن یا تغییر محصولات:

1. فایل `frontend/products.json` را باز کنید
2. ساختار هر محصول به این صورت است:

```json
{
  "id": 1,
  "title": "نام محصول",
  "shortDescription": "توضیح کوتاه",
  "description": "توضیح کامل محصول",
  "price": 45000,
  "image": "assets/combo1.jpg",
  "items": [
    "آیتم 1",
    "آیتم 2",
    "آیتم 3"
  ]
}
```

3. محصول جدید را به آرایه اضافه کنید یا محصول موجود را ویرایش کنید
4. برای هر محصول جدید، یک تصویر در پوشه `frontend/assets/` قرار دهید
5. فایل را ذخیره کنید و صفحه را رفرش کنید

### نکات مهم:
- `id` باید یکتا باشد
- `price` به تومان است
- `image` مسیر تصویر نسبت به فایل `index.html` است
- `items` لیست آیتم‌های داخل کمبو است

## 🔗 تنظیم Formspree برای دریافت سفارشات

برای دریافت سفارشات به صورت خودکار:

1. به سایت [Formspree.io](https://formspree.io) بروید
2. یک حساب کاربری بسازید (رایگان)
3. یک فرم جدید ایجاد کنید
4. آدرس endpoint را کپی کنید (مثل: `https://formspree.io/f/abc123xyz`)
5. فایل `frontend/app.js` را باز کنید
6. خط مربوط به `formspreeUrl` را پیدا کنید
7. آدرس endpoint خود را جایگزین کنید

### تنظیم Fallback (mailto)

در صورت خطا در ارسال به Formspree، سیستم از mailto استفاده می‌کند:

1. فایل `frontend/app.js` را باز کنید
2. خط مربوط به `email` را پیدا کنید
3. ایمیل خود را جایگزین کنید

## 🎨 سفارشی‌سازی

### تغییر اطلاعات تماس در هدر

فایل `frontend/index.html` را باز کنید و اطلاعات تماس را ویرایش کنید.

### تغییر رنگ‌ها

فایل `frontend/styles.css` را باز کنید و متغیرهای CSS در بخش `:root` را تغییر دهید.

## 📱 سازگاری

این پروژه با تمام مرورگرهای مدرن سازگار است و کاملاً ریسپانسیو است.

## 🛠️ تکنولوژی‌های استفاده شده

- **HTML5**: ساختار صفحه
- **CSS3**: استایل‌دهی و طراحی ریسپانسیو
- **JavaScript (ES6)**: منطق برنامه و تعاملات
- **localStorage**: ذخیره سبد خرید
- **Fetch API**: بارگذاری محصولات از JSON
- **Formspree**: دریافت فرم‌های سفارش

---

**موفق باشید! 🎉**

